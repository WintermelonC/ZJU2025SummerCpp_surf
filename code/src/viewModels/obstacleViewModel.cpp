#include "obstacleViewModel.h"

ObstacleViewModel::ObstacleViewModel() 
    : m_gen(m_rd()) {
    initialize();
}

void ObstacleViewModel::update(const float& dt) {
    
}

void ObstacleViewModel::initialize() {
    // 初始化障碍物组模式
    
}